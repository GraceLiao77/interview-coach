# Dependencies
node_modules/

# Build output
dist/

# Secrets / env — ignore all env files except the committed template
.env
.env.*
!.env.example

# Logs
*.log

# OS / editor junk
.DS_Store
.vscode/
.idea/

# Claude Code personal overrides (keep private, don't commit)
.claude/settings.local.json
.claude/CLAUDE.local.md

# Prisma generated client (if output outside node_modules)
/generated/prisma

# Personal interview prep notes (not part of the product)
/interview/

# Diary notes kept local: fundamentals that don't belong in a public portfolio repo
docs/diary/local/
